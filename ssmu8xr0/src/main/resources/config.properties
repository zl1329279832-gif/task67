# ============================================================
# Database Configuration — override via environment variables
# Set DB_URL / DB_USERNAME / DB_PASSWORD before starting Tomcat,
# or configure a JNDI DataSource (see spring-mybatis.xml).
# ============================================================

validationQuery=${DB_VALIDATION_QUERY:SELECT 1}

# --- MySQL (default) ---
jdbc_url=${DB_URL:jdbc:mysql://127.0.0.1:3306/ssmu8xr0?useUnicode=true&characterEncoding=UTF-8&tinyInt1isBit=false}
jdbc_username=${DB_USERNAME:root}
jdbc_password=${DB_PASSWORD:123456}

# --- SQL Server (switch via -P sqlserver) ---
#jdbc_url=${DB_URL:jdbc:sqlserver://127.0.0.1:1433;DatabaseName=ssmu8xr0}
#jdbc_username=${DB_USERNAME:sa}
#jdbc_password=${DB_PASSWORD:123456}

