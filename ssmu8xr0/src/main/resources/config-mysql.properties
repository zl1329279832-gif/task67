# ============================================================
# 白云会议管理系统 - MySQL 数据库配置
# 通过 Maven Profile 激活: mvn clean package -Pmysql
# 运行时可通过 JVM 参数覆盖: -Djdbc_url=jdbc:mysql://prod:3306/ssmu8xr0
# ============================================================
validationQuery=SELECT 1

jdbc_url=jdbc:mysql://127.0.0.1:3306/ssmu8xr0?useUnicode=true&characterEncoding=UTF-8&tinyInt1isBit=false
jdbc_username=root
jdbc_password=123456
